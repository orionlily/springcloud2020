server:
  port: 6525

spring:
  application:
    name: cloud-alibaba-payment
  cloud:
    nacos:
      server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: "*"